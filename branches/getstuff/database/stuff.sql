create table stuff(
id int NOT NULL AUTO_INCREMENT,
PRIMARY KEY(id),
time date NOT NULL,
mass text,
med text,
comp text,
let text,
lod text,
thought text,
ordo text,
ves text,
saint text,
valid int,
lastupdate date
)
